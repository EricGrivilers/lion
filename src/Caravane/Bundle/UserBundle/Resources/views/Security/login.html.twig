{% extends "CaravaneCmsBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}


{% block content %}

		<!-- Content area 4 -->
	<aside class='wrapper border-tb-white marble-color' id="detail">
			<div class="container main">

                <div class="col-md-4 col-md-offset-1">


        				{% block fos_user_content %}
                            {% if error %}
                                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                            {% endif %}

                            <form action="{{ path("fos_user_security_check") }}" method="post">
                                <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                                <div class="form-group">
                                    <label for="username">{{ 'security.login.email'|trans }}</label>
                                    <input type="text" id="username" name="_username" value="{{ last_username }}" required="required" class="form-control" />
                                </div>
                                <div class="form-group">
                                    <label for="password">{{ 'security.login.password'|trans }}</label>
                                    <input type="password" id="password" name="_password" required="required" class="form-control" />
                                </div>
                                <div class="form-group">
                                    <input type="checkbox" id="remember_me" name="_remember_me" value="on" class="form-control" />
                                    <label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>
                                </div>
                                <div class="form-group">
                                    <input type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" class="form-control btn btn-red" />
                                </div>
                            </form>
                            <p>
                                <a href="{{ path('fos_user_resetting_request') }}">{{"Mot de passe oublié?"|trans}}</a>
                            </p>
                        {% endblock fos_user_content %}
                    </div>
                    <div class="col-md-4 col-md-offset-2">
                        <div class="well">
                            <p>Pas encore de compte ?</p>
                            <p>Créez votre profil sur notre site et vous pourrez sauvegarder les biens qui vous intéressent ainsi que vos critères de recherches. Vous retrouverez les nouveautés qui vous intéressent  directement sur notre page d’accueil  lors de votre prochaine visite.</p>
                            <p><a href="{{path('fos_user_registration_register')}}" class="btn btn-lg btn-orange" >Créer mon profil</a></p>
                        </div>
		</div>
    </div>
	</aside>




{% endblock content %}
